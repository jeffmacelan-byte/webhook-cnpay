{
  "name": "cnpay-webhook",
  "version": "0.1.0",
  "private": true,
  "description": "Webhook handler para notificações de pagamento da CN Pay hospedado na Vercel.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "lint": "eslint . || echo \"eslint não configurado\"",
    "test": "node --test || echo \"sem testes definidos\""
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

